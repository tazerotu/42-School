Execution :
	- exit code
	- env -> heap use after free (free_cmd)
	- export -> substr leak
	- cd -> if previous file rm shouldn't break
	- unset -> doesn't work

Parser :
	- split redir (ne fonctionne pas avec le heredoc)
	- no exit code to 2 when parsing error
	- do not expand if here doc delimiter
	- if redirection symbol followed by nothing, it breaks

All :
	- history
	- free -> not everything is freed
	- Signals -> nothing done yet
	- Norm

	- export -> env -> heap use after free (free_cmd)