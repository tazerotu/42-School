Execution :


Parser :
	- split redir (ne fonctionne pas avec le heredoc, 
				leaks when exit/multiple command in a row, to fix in main together)
	- process line (leaks -> to be fixed in main together)
	- fill_cmds (leaks -> to be fixed in main together)

All :
	- free -> not everything is freed