Execution :
	exit -> if no arg, exit should be exit = 0
	exit -> too many arg -> no exit -> exit code = 1
	priorise arg 1 (num)
	exit -> int overflow (long)
	exit | exit should not exit

	export (vefrifier egal) pour env (afficher si = present, ne pas afficher si aucun egal)

	cd .. not handled correctly if previous rm

Parser :

All :
	- Norm -> to be verified