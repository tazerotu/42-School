Execution :
	- exit code
	- env -> heap use after free (free_cmd)
	- export -> substr leak
	- cd -> if previous file rm shouldn't break
	- unset -> doesn't work
	- cannot launch another minishell within minishell

Parser :

All :
	- free -> not everything is freed, or frees do too much
	- Signals -> nothing done yet
	- Norm

	- export -> env -> heap use after free (free_cmd)